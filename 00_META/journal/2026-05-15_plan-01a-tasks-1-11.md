# 2026-05-15 — Plan 01a Tasks 1-11 executed under subagent-driven-development

**Session type:** work · execution
**Duration:** ~6 hours focused
**Agent:** Claude (Opus 4.7, 1M context) at root, driver session with Ricardo
**Commits this session:** 16 task + hardening commits (`af908d5`, `4bb6490`, `5709af7`, `15539cd`, `8453148`, `91b5c9c`, `8a432c6`, `c454031`, `7ea853d`, `789eea2`, `50a4504`, `d941d55`, `c258466`, `f72d382`, `e5fd40d`, `f747312`) plus session-end commit
**Repo state at session end:** 17 commits ahead of pre-session baseline (`952cf2d`), pushed to Gitea origin; working tree clean; on `main`; at the JP-coordination gate between Tasks 11 and 12.

## What was done

This session executed Plan 01a Tasks 1-11 via `superpowers:subagent-driven-development`. Each task ran the full skill loop: implementer subagent (Sonnet) → spec compliance reviewer subagent → code quality reviewer subagent → conditional hardening commit for legitimate Important findings → mark complete → next task. Ricardo locked the operating principles at session start: do-it-right-do-it-once (no compressed audits, no skipped spot-checks, no shortcuts), execute on `main` directly, Sonnet for all implementers, reviewers stay on the controller's tier.

**Pre-flight (before any dispatch).** Ran the 17-line patch-verification spot-check from `00_META/proposals/2026-05-14_plan-01a-patch-verification-trail.md` — all 17 checks landed on the expected counts, confirming the 7 HIGH patches from `6ca022c` were still live (no intersessional drift). Plan's own pre-Task-1 checks (cwd, working tree clean, age 1.1.1, pandoc 3.1.3, jsonschema 4.10.3, PyYAML 6.0.1) also passed.

**Tasks executed (each line is one task + its hardening, if any):**

- **Task 1 — Folder scaffold** (`af908d5`). 3-bucket layout: 55 directories + 55 `.gitkeep` placeholders. One DONE_WITH_CONCERNS surfaced: `infra/secrets/.gitkeep` needed `git add -f` because the interim `.gitignore` carries the `*secrets*` blanket glob (patched in Task 2). Force-add was the right call.

- **Task 2 — Comprehensive `.gitignore`** (`4bb6490` + `5709af7` hardening). Replaced interim minimal gitignore with comprehensive F23 version: surgical secret patterns (no `*secrets*` blanket glob per Finding 1 patch), explicit `!secrets.env.age` + `!infra/secrets/MANIFEST.md` allowlist, Python/Node/IDE/OS/Docker/vault sanity blocks. Coverage-test script `infra/scripts/test_gitignore_coverage.sh` runs 20 checks. **Hardening:** reviewer caught (a) tautology — `grep -qE` was matching commented-out patterns; filter `^\s*(#|$)` before grep; (b) missing `set -euo pipefail` + file-exists guard. Negative-control proved the tautology fix works (commenting `*.key` causes "Age private keys" check to FAIL).

- **Task 3 — Pre-commit secret-scan hook** (`15539cd` + `8453148` hardening). Hook at `infra/hooks/pre-commit-secret-scan.sh` symlinked into `.git/hooks/pre-commit`. Scans STAGED BLOB via `git show :$f` per Finding 3 patch (NOT on-disk). Step 6b live-tested the stage-then-edit-clean bypass — hook blocks it correctly, exit 1. **Implementer deviation noted:** test harness literal `sk-ant-` + `test1234567890abcdef` (full string elided to avoid tripping the hook on this very journal entry, which the hook would otherwise correctly flag) would trip the hook on commit; resolved by splitting into `SECRET_PREFIX="sk-ant-"` + `SECRET_SUFFIX` concatenated at runtime. Runtime planted file still contains full secret; hook still triggers. (This journal entry uses the same split-pattern trick.) **Hardening:** I1 misleading case-statement comment, I2 stdin mode bypasses `scan_content` function (pattern-duplication risk), I3 silent fail if git not in PATH, I4 `\b` anchor on `sk-ant-` for consistency. Negative-control verified the missing-git guard fires correctly (`PATH="/tmp/empty_bin" /bin/bash …`).

- **Task 4 — `pipeline/clients/_template/`** (`91b5c9c`). 3 files: `state.json` (nexostrat-client-state-v1 schema; phase=prospect; phase_history seeded), `checkpoint.md` (CHECKPOINT_NO_ACTIVE_WORK token per ADR-031), `README.md` (12 stations + 3 cross-cutting documented). Reviewer flagged the Notion reference in transcripts/ row as Important — declined to fix per `t-spec-notion-removal-amendment` (the single-pass cleanup covers all Notion sites coherently; piecemeal mid-execution edits would create incoherence).

- **Task 5 — JSON Schemas (F21)** (`8a432c6` + `c454031` hardening). `nexostrat-tasks-v1` + `nexostrat-calendar-v1` schemas at `infra/schemas/` + validator `infra/scripts/validate_schemas.sh`. Both files validate without data patching. **Hardening:** reviewer caught Important — `jsonschema.validate(...)` does NOT enforce `format:` keywords by default; date/date-time annotations were dead. Switched to `Draft202012Validator(schema, format_checker=FormatChecker())` + `iter_errors`; FAIL output now includes the JSON path (e.g., `$['tasks'][3]['created']`). Negative-control proved the fix: corrupting `tasks[0].created` to "not-a-date" now FAILs.

- **Task 6 — Per-machine YAML profiles + bootstrap-machine.sh** (`7ea853d` + `789eea2` hardening). All 7 profiles per spec §5: hp-server, hp-standby, ricardo-desktop, ricardo-travel, jp-light, jp-heavy, phones. `jp-heavy.yaml` carries the CHECKPOINT-mandated ADR-038 stub annotation as line 2 ("future-state stub; JP currently Light per ADR-038 (2026-05-15)…"). Bootstrap skeleton parses + summarizes profiles; smoke-tested against hp-server, phones, ricardo-desktop, nonexistent (exit 1). **Hardening:** drop unused `json` import in Python heredoc, clarify exit-2 reservation ("Plan 02 will use this for unsupported-OS detection") since no code path emits it yet.

- **Task 7 — Skills migration** (`50a4504`). 4 skills moved `00_META/skills/{company-analyst,industry-analyst,competitor-analyst,discovery-meeting}/` → `skills/{01_company_analyst,02_industry_analyst,03_competitor_analyst,06_discovery_meeting}/` via `git mv`. Empty `04_meeting_script/` + `05_opportunity_report/` placeholders preserved. `00_META/skills/` directory removed via `rmdir`. Finding 5 fix in action: `git add -A` instead of bare `git add 00_META/skills` (which would have errored fatal). 17 pure renames (R100); zero content modifications. No hardening needed.

- **Task 8 — Questionnaire migration (F15)** (`d941d55`). Pandoc-converted both Plan Maestro questionnaires `.docx → .md` (gfm, --wrap=preserve): `2026-05-07_ricardo.md` (1038 lines) + `2026-05-07_jp.md` (965 lines). Both files prepended with YAML frontmatter (source/respondent/date/converted). 3 docx files moved to `00_PARTNERSHIP/questionnaires/archive/`; `Consultoria_IA_PYMEs_v1.pdf` moved to `operations/assets/`. Root now clean of docx/pdf clutter. Reviewer flagged Important — raw HTML tables from pandoc's fallback rendering of complex Word tables — inherent pandoc behavior, not a defect; declined to deviate.

- **Task 9 — `00_PARTNERSHIP/` policy files** (`c258466` + `f72d382` hardening). 7 markdown files: CONFLICT_PROTOCOL.md (JP's 15-min raised-hand mechanism), REVENUE_DISTRIBUTION.md (20/20/20/40), ROLES.md, KPIs.md, cost-sharing-agreement.md, qualified-prospect-definition.md, raised_hand_log.md (empty stub). PARTNERSHIP_AGREEMENT.md deferred to Task 17. **Hardening:** reviewer caught Important — `cost-sharing-agreement.md` Ricardo/JP total rows were ambiguous (`~USD 215/mo + Claude MAX 200` left whether 215 included Claude MAX). Rewrote both totals: Ricardo `USD 215-225/mo (Claude MAX 200 + Grok 5-15 + Drive 10)`, JP `USD 237-257/mo (Claude MAX 200 + Notion 30-50 + Domain ~1 + Email 6)`. Bitwarden ($1, non-reimbursable) correctly excluded.

- **Task 10 — `vault/` README + sensitive_index** (`e5fd40d` + `f747312` hardening). `vault/README.md` (84 lines) documents F10 namespace split + rare-access discipline + recovery scenarios + add-new-recipient one-liner. `sensitive_index.md` (41 lines) is the catalog template. **Hardening:** reviewer caught Important — vault README command snippets referenced `~/.config/age/nexostrat.key` (plaintext path that doesn't exist) while Recovery section correctly cited `~/.config/age/nexostrat.key.age`. Anyone following the runbook literally would hit "no such file". Fixed both occurrences to use the `.age` path (age 1.1.1 supports passphrase-protected identity files directly via `-i`).

- **Task 11 — Verify Ricardo's age setup** (no commit — verification-only). Step 1 ✅ both pubkeys present in `infra/age-recipients.txt` (Ricardo + JP). Step 2 ✅ `nexostrat.key.age` mode 600, owned by ricardo. Steps 3 + 4 required interactive passphrase prompts; ran in a regular terminal alongside the session (the `!` shortcut and process substitution both fail TTY-less). Step 3 ✅ passphrase decrypts the identity to a valid age secret-key file. Step 4 hit a plan-text bug — `<(age -d <encrypted-id>)` process substitution can't read passphrase in a subshell. Rewrote with `age -d -i <encrypted-id>` directly (same pattern as the Task 10 hardening fix) — ✅ ROUNDTRIP PASS. Plan-text bug logged to `t-plan-01a-text-amendments`. Step 5 STATUS.md update done as part of session-end commit (per plan, Task 11 has no commit of its own).

**Subagent statistics (rough):** ~33 subagent dispatches total — 11 implementers (Sonnet), 11 spec reviewers (Sonnet), 11 code quality reviewers (Sonnet), plus the 6 hardening implementers (Sonnet). Each task averaged ~3 reviews + ~1 hardening dispatch. No BLOCKED status surfaced; all NEEDS_CONTEXT moments resolved with single-message context handoffs.

## Decisions made

- **Operating principle locked at session start: do-it-right-do-it-once.** Reaffirmed Ricardo's framing from 2026-05-15: no compressed audits, no skipped spot-checks, no shortcuts. Drove the entire session: pre-flight 17-check spot-check ran (not skipped), every task got both reviews (not folded), every load-bearing Important got a hardening commit (not deferred).

- **Direct-to-main branch strategy.** No feature branch, no worktree. Single-developer foundation work where the "code" IS the repo scaffolding; isolation would have added ceremony without protecting anything. Per-task commits keep `main` in a clean working state throughout.

- **Sonnet for all 11 implementers + reviewers.** Plan is well-specified (3523 lines post-patch); most tasks are mechanical. Reviewers stay on Sonnet too — adversarial probes mostly require careful reading, not architectural reasoning. Cost-efficient; no quality regressions noticed.

- **Hardening commit discipline.** Reviewer "Important" findings that were both legitimate engineering gaps AND scoped within the task's deliverable got a follow-up commit. Reviewer findings that touched the prescribed plan content (Notion references, raw HTML tables from pandoc, vault block extension list) were declined — those wait for the canonical single-pass touchups (`t-spec-notion-removal-amendment`, `t-spec-cost-table-amendment`, `t-plan-01a-text-amendments`). Piecemeal mid-execution patches would create cleanup-debt incoherence; canonical sweep is the do-it-once vehicle.

- **Plan fidelity over reviewer-driven content edits.** Reviewers flagged stale-state items (Notion in ROLES.md, transcripts canonical=Notion AI in `_template/` README, Whisper.cpp in hp-server services) — kept all as plan-prescribed and logged for `t-spec-notion-removal-amendment`. The Notion-cleanup task is intentionally scheduled for a single-pass commit between Plan 01c execution and Plan 02 writing; touching it now would split the cleanup across N commits and N reviewers.

- **End session at the gate.** Plan 01a's design has the JP-coordination gate between Tasks 11 and 12; Ricardo confirmed End Session rather than pressing past it. Tasks 12-18 in next session (~1-2h elapsed including JP Direction B Signal-attachment turnaround).

## Open items

**Concrete next action (next session):**
- Resume Plan 01a at Task 12. Note Task 12 may collapse to verify-only since JP's pubkey is already on file (commit `b7e39bf`). Task 13 Direction A (git push) + Direction B (Signal-attachment per Finding 6 patch) is the only real coordination step. Tasks 14-18 are mechanical (secrets.env.age, run-with-secrets.sh, MANIFEST.md, partnership PDF, final verify + tag).

**Downstream:**
- Tag `v0.1a-foundation` on Plan 01a completion.
- Plan 01b re-audit + execute → tag `v0.1b-mirrors`. Tasks 7-12 of 01b gate on physical second host.
- Plan 01c re-audit + execute → tag `v0.1-foundation`.
- Single-pass plan-text amendments commit: `t-spec-notion-removal-amendment` + `t-plan-01a-text-amendments` + `t-spec-cost-table-amendment`. Scheduled between Plan 01c execution and Plan 02 writing.
- Plan 02 brainstorm + write + audit + execute (FOSS replacements for Notion's 4 roles per `t-foss-docs-stack-decision`).

**Deferred per-task Minor code-quality findings** (collected for a post-01c polish pass):
- Task 2: vault block extension gaps (.json/.yaml/.csv/.xlsx/.png/.jpg not covered), dead `/dev/shm` glob (anchored to repo root), `*~bak` likely typo, `env/` over-broad.
- Task 3: false-positive allowlist mechanism, JWT pattern false-positive rate, performance for 500-file commits.
- Task 6: jp-light hooks-empty inline comment, phones.yaml schema divergence note, missing-arg profile list path.
- Task 9: source attribution on ROLES.md (asymmetric with the other 6 files), forward-references for `pipeline/prospects/` + `operations/accounting/api-spend.md` + `00_PARTNERSHIP/reviews/`.

**No active blockers.** Working tree clean post-session-end commit. Origin push pending in session-end batch.

## Notes

**For the next session reading this cold:** the session executed Plan 01a Tasks 1-11 cleanly. 16 task + hardening commits landed on `main`. The plan is now half-done; tag `v0.1a-foundation` is 1-2 hours away once JP completes the Direction B Signal-attachment exchange. The pause is by design (Plan 01a's built-in coordination gate), not because anything is broken.

**The subagent-driven-development pattern worked well.** Two-stage review per task caught real Important issues 6 times across 11 tasks — that's a 55% hit rate on the hardening loop. Most of those were engineering hygiene (set -e missing, dead imports, ambiguous total rows, wrong file paths in command snippets); none were architectural defects. The plan author's intent survived intact; the reviewers improved the implementation around the edges.

**The 17-check pre-flight spot-check was cheap insurance and worth running again next session.** It takes ~5 seconds and proves the 7 HIGH patches from `6ca022c` are still where they're supposed to be. Drift between sessions is the cheapest class of bug to detect with this kind of forensic-grade companion doc.

**The plan-text bugs in Task 11 Steps 3 + 4 are the only real plan-text drift surfaced this session.** Both Step-3 (line-1-content) and Step-4 (process substitution vs TTY) are documentation-grade issues, not behavior-grade. They were caught by live execution and logged in `t-plan-01a-text-amendments`. No other plan-text drift surfaced — the patched plan held up well under execution.

**Things this session did NOT do (intentionally):**
- Did not patch `t-spec-notion-removal-amendment` sites piecemeal as reviewers flagged them. Single-pass touchup remains the right vehicle.
- Did not run the patch-verification-trail spot-check at every task transition (only at session start). The 17 checks are about the 7 HIGH plan patches surviving intersessional drift; intra-session drift between Task 5 and Task 7 doesn't shape the same risk surface.
- Did not push to origin between commits. Push happens once at session end (current Gitea origin, mirrors come in Plan 01b).
- Did not add `Co-Authored-By` trailers to any task or hardening commit. Reserved for session-end and ad-hoc commits where Ricardo + Claude collaborated on the message directly.

**Cross-session coherence check:** this session connects to the prior `2026-05-15_jp-onboarding-and-adr-038.md` session (which left the baton at "execute Plan 01a Tasks 1-11 NEXT") via the pre-flight spot-check + dispatch sequence the prior CHECKPOINT prescribed verbatim. Every locked-decision from that CHECKPOINT held during execution: JP stayed Light, Notion stayed out, GitHub `nexostrat` org wasn't re-created, `jp-heavy.yaml` got its stub annotation. No re-litigation. Direction unchanged.
